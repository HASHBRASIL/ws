<script type="text/javascript" src="//static.jstree.com/3.2.1/assets/dist/jstree.min.js"></script>
<script type="text/javascript" src="{{view.baseUrl()}}/js/treeFunction.js"></script>
<link rel="stylesheet" type="text/css" href="//static.jstree.com/3.2.1/assets/dist/themes/default/style.min.css"/>

<div id="loader">
    <div>
        <i class="fa-li fa fa-cog fa-spin"></i>
    </div>
</div>
<div class="row box-form-container">    
    <div class="col-md-6">
        <form class="form-tree" method="POST" action="">
            <select name="idTimeEscolhido" class="select-times">
                {% for t in times %}
                    <option value="{{ t.id }}" {% if t.id == idTimeEscolhido %} selected {% endif %}>
                        {{ t.nome }}
                    </option>
                {% endfor %}
            </select>

            <select name="idTipoEscolhido" class="select-tipo">
                <option value="grupos" {% if 'grupos' == idTipoEscolhido %} selected {% endif %}>
                    grupos
                </option>
                <option value="servicos" {% if 'servicos' == idTipoEscolhido %} selected {% endif %}>
                    serviços
                </option>
            </select>

            <input type="submit" value="Atualizar">

        </form>


        <div>
            {#<button type="button" type="button" class="btn btn-sm btn-info add_node">incluir</button>
            <button type="button" class="btn btn-sm btn-danger delete_node">excluir</button>
            <button type="button" class="btn btn-sm btn-warning rename_node">renomear</button>#}
            <input type="text" class="busca-tree" value="">
        </div>

        <div id="jstree"></div>
    </div>
        
    <div class="col-md-6 editar-propriedade">
        
    </div>
</div>